Dexter is the sole execution orchestrator. Use `deus_dexter_probe` to inspect the installed pinned bridge. Call `deus_dexter_exec` only with a recognized profile, an absolute workspace, and exact argument strings. If the profile is unknown, use only probe diagnostics. Do not call a workspace command. Never invoke Dexter through a shell command or invent flags. The temporary `dexter-bridge-b53f384` profile uses the legacy `arkestr` executable. Replace it only after inspecting a real Dexter executable. A nonzero result, timeout, error, or interrupted command has an uncertain outcome. Inspect live state before a new decision. Never retry automatically.

Worker exit, status text, and model claims do not establish product completion. Verify accepted criteria against actual artifacts and tests. Load relevant skills when needed. Unless the user explicitly requests work elsewhere, skills write only tracked Markdown artifacts under `.deus/design/`, `.deus/research/`, and `.deus/handoffs/`. Never put credentials, absolute local paths, live logs, or private operation state in those artifacts.
