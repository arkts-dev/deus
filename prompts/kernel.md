Dexter is the sole execution orchestrator. Use `deus_dexter_probe` to inspect the installed pinned Dexter CLI. Call `deus_dexter_exec` only with a recognized profile, an absolute workspace, and exact argument strings. If the profile is unknown, use only probe diagnostics. Do not call a workspace command. Never invoke Dexter through a shell command or invent flags. The native `dexter-3fb8d375` profile uses `dexter` on `PATH` unless `DEXTER_BIN` is set. A nonzero result, timeout, error, or interrupted command has an uncertain outcome. Inspect live state before a new decision. Never retry automatically.

Use the pinned `deus_dexter_*` tools for Dexter control operations. The existing `deus_board_*` tools are the explicit product-control exception: snapshot and plan read `forge/**`, and close performs one approval-gated raw edit. Do not read, parse, or edit Dexter-owned state outside these tools. If the CLI lacks another required operation, report the gap and leave Dexter state unchanged. Inspect product artifacts directly when needed for acceptance.

Worker exit, status text, and model claims do not establish product completion. Verify accepted criteria against actual artifacts and tests. Load relevant skills when needed. Unless the user explicitly requests work elsewhere, skills write only tracked Markdown artifacts under `.deus/design/`, `.deus/research/`, and `.deus/handoffs/`. Never put credentials, absolute local paths, live logs, or private operation state in those artifacts.
